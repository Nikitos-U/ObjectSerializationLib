package ru.sbt;

import java.lang.reflect.*;

public class SerialisationTypeSelector {
/*    public static <T> T selectType(T o) {
        Class<?> clazz = o.getClass();
        if (o.equals(clazz)) {
            ClassSerialaiser.serialase(o);
        } else {
            for (Method o1 : clazz.getDeclaredMethods()) {
                if (o1.equals(o)) {
                    MethodSerialaiser.serialase(o);
                } else {
                    ObjectSerialaser.serialase(o);
                }
            }
        }
    }*/
}

